package edu.berkeley.cs.scads

/*
abstract class MulticastCluster(dp: DataPlacement) extends ClusterMembership(dp) {
	// TODO
}

abstract class QuorumClientLibrary(read_q: Int, write_q: Int, dp: DataPlacement) extends ClientLibrary(dp) {
	/* State */
	def read_q: Int 	// how many nodes to read from before returning result
	def write_q: Int	// how many nodes to write to before returning success

	/* Methods */
	def get(namespace: String, key: String): Record 						// read from quorum nodes
	def get_set(namespace: String, keys: RecordSet): java.util.List[Record]			// read from quorum nodes
	def put(namespace: String, rec:Record): Boolean 	// write to quorum responsible nodes
}
*/
